# Base URL of the FastAPI backend. The backend must already be running
# separately (uvicorn app.main:app --reload) — this frontend never starts,
# mocks, or modifies it.
VITE_API_BASE_URL=http://127.0.0.1:8000 
